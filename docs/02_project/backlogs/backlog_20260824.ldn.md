# LDN · WClock5 · docs/backlogs/backlog_20260824.md
- ver: 0.1
- updated: 2026-08-24
- original: new
- status: 🔄 в работе

## Сводка
День (24.08): **метки периода: правый край, предыдущий период, area-режим графика**.

## План на сегодня (24.08)

### Цель №1 — Метки периода: предыдущий период и выравнивание
- UI-01 ✅ (H) — `invest_repo.py`: `_prev` блок в `read_history()` — запрос по
  `portfolio_positions` + `portfolio_hourly` за предыдущий период
  (`prev_cutoff..cutoff`), возвращает `{source: {start, end}}` для каждой
  стратегии
- UI-02 ✅ (M) — `invest_chart.js`: фильтр `_prev` из `Object.keys(rawData)`,
  вычисление `tinkoffPrevDelta`/`finamPrevDelta` из `_prev[source].end` →
  `((currentStart - prevEnd) / prevEnd) * 100`; передача в `periodSources`
  через `prevDelta`; метка начала: `10,800М +0,67%`
- UI-03 ✅ (S) — выравнивание всех меток (старт + финиш) по правому краю
  (`LABEL_RIGHT_X = chartArea.left + 78`)

### Цель №2 — Исправления и мелкий UX
- FIX-01 ✅ (M) — `invest_banner.js`: фильтр `_prev` из ключей
  `Object.keys(historyData)` — баннер ломался (`_prev` → последний ключ после
  сортировки → `Array.isArray` = false → «Нет позиций»)
- UX-01 ✅ (S) — `control_panel.js`: добавлен «3 дня» (`-3 day`) в
  `panel-view-select` + автоинтервал `hour`

### Цель №3 — tg_turnover_daemon: edited_message (перенос с 21.08)
- FIN-01 ✅ (H) — `tg_turnover_daemon.py`: обработка `edited_message` /
  `edited_channel_post` (корень: бот редактирует посты, демон читал только
  `channel_post`/`message`); фикс `lab_inspetor` → `lab_inspector`; upsert
  по `(day, source)` — обновление `strategy_summary` для текущего дня;
  улучшенное логирование

### Цель №4 — Баннер: обороты и комиссия (перенос с 21.08)
- FIN-02 ✅ (M) — `app.py`: `/api/invest/turnover` — fallback для Finam через
  `finam_commission_estimate()` + `round(total_fb * 0.0002, 2)`
- FIN-03 ✅ (S) — `invest_banner.js`: cache-busting `&_=` на turnover API;
  `buildFinamTariffTip(total)` — CSS-тултип (position fixed, z-index 99999) с
  тарифной таблицей и подсветкой активного брекета
- FIN-04 ✅ (S) — CSS: `.finam-tariff-css-tip` (position fixed), `.finam-tariff-active`

### Цель №5 — Графики: стиль и метки (перенос с 22–23.08)
- CH-01 ✅ (M) — `PeriodLabelsPlugin`: 3 знака после запятой (`10,800М`);
  формат `toFixed(3)` + русская замена `.→,`; длина подписи ~50px max
- CH-02 ✅ (M) — `banner-turnover-block` цвет `#effcfc`; семантические классы
  `.finam`/`.tinvest` на `<table>`

### Цель №6 — Area-режим графика (в работе)
- AREA-01 ⏳ (H) — `lib.js`: переключатель «линия / область» в
  `panels-modal-content` → настройка `invest_render_mode` (`line`/`area`)
- AREA-02 ⏳ (H) — `invest_chart.js`: плагин area — полоса капитала, высота
  пропорциональна доле TGLD (100% = 20px, 0% = 1px)

## Открыто
- [ ] AREA-02 (H) — доработать area-плагин: данные о доле TGLD per-point из API
- [ ] FIN (S) — живые обновления Финама: вторая группа-зеркало, связать с
  каналом «…Финам (TGLD)», добавить бота (нужно действие владельца)
- [ ] UI (M) — сплит инвест-баннера на 3 drag&drop панели
- [ ] PERF (M) — лаг TGLD ~1 мин на графике: адаптивный интервал
- [ ] QA (S) — контроль графика на планшете после чистки дублей
- [ ] TGLD/TMON/XAU (M) — перенос котировок на Finam Bars/LastQuote

## Связи
- Предыдущий: `backlog_20260821.md` (UI-01..05, CH-01..05, DOC-01, FIN-01..07)
- Конвенции: `docs/02_project/conventions.md`
- Исходники: `invest_repo.py`, `invest_chart.js`, `invest_banner.js`,
  `control_panel.js`, `tg_turnover_daemon.py`

## Лист изменений
- **0.1** · 2026-08-24 — создан: метки периода (_prev, prevDelta, правый край),
  _prev-фикс баннера, «3 дня», tg_turnover daemon edited_message, area-toggle
