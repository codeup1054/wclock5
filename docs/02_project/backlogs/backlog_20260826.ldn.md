# LDN · WClock5 · docs/backlogs/backlog_20260826.ldn.md
- ver: 0.3
- updated: 2026-08-28
- original: new
- status: 🔄 в работе

## Сводка
День (26.08): **T+F сводные графики (₽ и %), легенда 3 столбца, §5.8 конвенций**.

## План на сегодня (26.08)

### Цель №1 — T+F сводные графики
- TF-01 ✅ (M) — `invest_chart.js`: датасет `T+F, ₽` — поэлементная сумма
  `tinkoffSeries + finamSeries`, `yAxisID: 'y_portfolio'`, hidden
- TF-02 ✅ (M) — `invest_chart.js`: датасет `T+F, %` — `%` рост суммы
  (`toPctSeries` от суммы), `yAxisID: 'y_pct'`, hidden

### Цель №2 — Легенда: группировка T+F
- LEG-01 ✅ (S) — `inlineLegendPlugin`: `T+F, ₽` и `T+F, %` автоматически
  попадают в столбец 2 (Тикеры/прочее) через `else`-ветку группировки
  (нет префикса `Ф:`/`Т:`)

### Цель №3 — Конвенции: §5.8 Легенда
- DOC-01 ✅ (S) — `conventions.md`: добавлен §5.8 «Легенда: drag&drop, touch,
  cookie» — стиль, 3 столбца, touch-параметры (5px threshold, 20×20 collapse),
  cookie (pos/collapsed/state), клампинг, подавление клика после drag,
  группировка столбцов

## Открыто
- [ ] AREA-02 (H) — доработать area-плагин: данные о доле TGLD per-point из API
- [ ] FIN (S) — живые обновления Финама: вторая группа-зеркало, связать с
  каналом «…Финам (TGLD)», добавить бота (нужно действие владельца)
- [ ] UI (M) — сплит инвест-баннера на 3 drag&drop панели
- [ ] PERF (M) — лаг TGLD ~1 мин на графике: адаптивный интервал
- [ ] QA (S) — контроль графика на планшете после чистки дублей
- [ ] TGLD/TMON/XAU (M) — перенос котировок на Finam Bars/LastQuote

## Цель №4 — Профили панелей (переключение макетов в модалке)

Вопросы владельца (перед планированием):
- Состав профиля: позиции + размеры + видимость (top/left/width/height + visible)
- Старт: 2 профиля — «Вариант 1» (текущий layout), «Вариант 2» (копия)
- При расхождении с текущими drag-изменениями — **спрашивать** до применения
- UI: управление в `panels-modal-content`, после строки invest_panel
- Хранение: localStorage + сервер (saveSettingsToServer)

### Задачи
- PF-01 ✅ (M) — `panel_resize.js`/`lib.js`: модель профилей —
  `wclock_panel_profiles` (localStorage), синк на сервер; снимок
  позиции+размеры+видимость всех панелей; `capturePanelConfig` выделен;
  merge с сервера при загрузке
- PF-02 ✅ (M) — `lib.js`: UI-блок в модалке после строки invest_panel:
  select профиля (Вариант 1/2) + кнопки «Применить» / «Сохранить как» /
  «В профиль»; строка рендерится модулем `panel_profiles.js`
- PF-03 ✅ (S) — apply профиля: `applyConfigToPanels` + видимость; при наличии
  несохранённых drag-изменений — confirm-диалог (`pp-confirm-modal`);
  индикатор «изменено» через MutationObserver за style панелей
- PF-04 ✅ (S) — деплой + проверка переключения и персистентности (F5 → состояние)

## Связи
- Предыдущий: `backlog_20260824.md` (UI-01..03, FIX-01, UX-01, FIN-01..04, CH-01..02, AREA-01..02)
- Конвенции: `docs/02_project/conventions.md`
- Исходники: `invest_chart.js`, `invest_banner.js`, `panel_resize.js`, `panel_configs.js`, `lib.js`, `panel_profiles.js`, `conventions.md`

## Лист изменений
- **0.3** · 2026-08-28 — PF-01..04 выполнены: профили панелей работают
  (select + Применить/Сохранить как/В профиль, confirm при несохранённых изменениях,
  localStorage+сервер), деплой на prod
- **0.2** · 2026-08-28 — Цель №4: профили панелей (2 варианта, размеры+видимость,
  localStorage+сервер, confirm при смене, UI после строки invest_panel)
- **0.1** · 2026-08-26 — создан: T+F datasets (₽, %), легенда 3 столбца, §5.8 конвенций
