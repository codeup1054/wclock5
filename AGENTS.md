# AGENTS.md — wclock5

## Bootstrap конвенций (кв)

Фреймворк: `E:\_dev\01.lab` · токены: `00_rules/prompting.md` §4.2
Конвенции проекта: `docs/02_project/conventions.md`
Беклоги: `docs/02_project/backlogs/` (актуальный — по дате в имени)

При старте сессии:
1. Прочитай три пути выше (токены, конвенции, свежий беклог).
2. Подтверди одной строкой: «кв активны: пп, пк, пл, пс, пу, кв».
3. Дальше применяй токены автоматически; без токена при неоднозначности —
   предложи варианты (`? применить {пп | пк}?`).

Краткая память по токенам: `пп` промптинг/метки · `пк` код · `пл` LDN ·
`пс` стиль+индексация · `пу` UI · `пш` паттерны/штриховка · `кв` этот файл.
Цифра = раздел (`пп4` — метки правок). `-п` после токена = справка.

Команды деплоя:
- `пп -дп` — деплой prod (VPS 217.114.8.5, порт 10405, `bash sh/devops.sh static`)
- `пп -дд` — деплой dev (локальный, без VPS)

## Специфика проекта

- Прод-сервер и деплой: см. conventions.md §1 (порт 10405, scp→/tmp→check).
- Инвест-данные: оборот = только стратегия (Telegram), время — ts_epoch.
- Git: remotes origin + vps; мусор не коммитить (=, логи, *.bak*, tools/).

### пш — паттерны/штриховка

- Конвенции: `docs/02_project/conventions.md` §5
- Функции: `hatchPattern`, `rgba`, `makeLayer` в `invest_chart.js`
- Параметры: tile 23×23, SPACING 18, lineWidth 1, dpr/4, HATCH_ALPHA 0.35
- Углы: T -45°, F 45°
- Быстрый деплой: `bash sh/deploy_js.sh`

### пд — деплой

- JS/CSS: `node --check` + `scp` (или `bash sh/deploy_js.sh`)
- Python: `scp` напрямую + `docker compose restart`
- MD5-проверка после каждого деплоя

### пб — беклог/эпики

- Беклог фреймворка: `01.lab/02_project/backlogs/PLN_backlog.md`
- Эпики: `01.lab/02_project/epics/`
- Конвенции: `01.lab/02_project/PROP_conventions.md` (P1–P9)
